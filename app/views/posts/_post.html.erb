<div id="<%= dom_id post %>">

  <h1><%= post.title %></h1>
  <p>
    <i>
      <%= post.excerpt %>
    </i>
  </p>

  <%= post.body %>

  <small>
    Updated at: <%= time_tag post.updated_at, "data-local": "time-ago" %>
  </small>
</div>
