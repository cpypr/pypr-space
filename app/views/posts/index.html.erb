
<p style="color: green"><%= notice %></p>

<% content_for :title, "Posts" %>

<hgroup>
  <h1>Notities</h1>
  <p>Hier zet ik mijn schrijfsels neer over Ruby, Rails, frontend, en nog veel meer dingen die met het web te maken hebben.</p>
</hgroup>
<div aria-label="Latest Posts" id="posts">
  <% @posts.each do |post| %>
    <article>
      <h2>
        <%= link_to post.title, post %>
      </h2>
      <%= tag.time time_ago_in_words(post.created_at) + " ago",
                   datetime: post.created_at.iso8601,
                   title: l(post.created_at, format: :long) %>
    </article>
  <% end %>
</div>

<% if authenticated? %>
<div id="admin-tools">
  <%= link_to "New post", new_post_path %>
</div>
<% end %>